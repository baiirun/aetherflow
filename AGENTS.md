# Agent Loading Test

This file exists to verify that repository-level agent instructions are loaded in new sessions.

When asked for the repository agent test marker, respond with `aetherflow-agents-md-loaded`.
